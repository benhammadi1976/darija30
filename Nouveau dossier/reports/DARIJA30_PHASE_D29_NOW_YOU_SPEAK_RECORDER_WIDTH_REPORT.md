# DARIJA30 Phase D29 — Now You Speak Recorder + Desktop Width

## Goal
Add a local voice recording practice flow and widen desktop layouts evenly.

## Implemented
- Added `Now You Speak` below `Show Moroccan chat spelling` in the left scenario/video column.
- Added browser-based recording using `MediaRecorder`.
- Added controls:
  - Record
  - Stop
  - Play My Voice
  - Try Again
- Added privacy note: recording stays on device and is not uploaded.
- Added unsupported-browser and microphone-permission messages.
- Applied the feature to every phrase page because the markup is generated by `lesson-player.js`.
- Expanded desktop width for lesson pages and app/content page containers while keeping centered margins.

## Placement rule
The recorder appears after the learner has seen:
1. Scenario
2. Goal
3. Darija30 Method
4. Video Dialogue
5. Show Moroccan chat spelling

Then the learner sees:
- Now You Speak

## Not changed
- No lesson content changed.
- No original audio paths changed.
- No video paths changed.
- No image assets changed.
- No backend/admin/lock logic changed.

## Validation
- `node --check js/data/lessons-data.js` passed.
- `node --check js/lesson-player.js` passed.
- `node --check js/router.js` passed.
- `node --check js/app.js` passed.
- `node --check js/progress-store.js` passed.
- Lesson count remains 30.
- Phrase count remains 150.
- Scene visual count remains 10.
