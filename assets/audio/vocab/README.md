# Vocab audio snippets

This folder is for single-word or short phrase audio used inside Blog phrase cards.

Rules:
- Use real Moroccan voice when possible.
- MP3, mono, 128–192kbps.
- File names should be lowercase and simple, for example: `salam-normal.mp3`.
- If a file is missing, the site will show “Recording needed” instead of failing silently.
